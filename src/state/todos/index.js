import initialState from './initial.state';

// Action types

// Action creators

// Selectors

// Reducers
export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
