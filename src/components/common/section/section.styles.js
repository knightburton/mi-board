export default theme => ({
  paper: {
    position: 'relative',
    padding: theme.spacing(3, 2)
  },
  marginTop: {
    marginTop: theme.spacing(3)
  },
  marginBottom: {
    marginBottom: theme.spacing(3)
  }
});
