import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  form: {
    position: 'relative',
    padding: 0,
    textAlign: 'center'
  },
  single: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-start'
  }
}));
